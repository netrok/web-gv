import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    mode: 'light',
    primary: { main: '#0B4F6C' },   // corporativo sugerido
    secondary: { main: '#01BAEF' },
  },
  shape: { borderRadius: 12 },
})
